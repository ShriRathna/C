#include<stdio.h>
int main()
{
   int a,b;
   printf("Enter the value for a and b:");
   scanf("%d %d",&a,&b);
   printf("Before swapping: a=%d b=%d",a,b);
   if (a!=b)
   {
      a=a+b;
      b=a-b;
      a=a-b;
    }
   printf("\nAfter swapping a=%d b=%d",a,b);
   return 0;
}
